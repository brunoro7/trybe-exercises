/** ======= Criando as constantes ======= */

const base = 5;
let height = 8;

/** ======= Criando a multiplicação entre base e height ======= */

const area = (base*height);
console.log(area);

/** ======= Criando uma const perimeter, e fazendo a soma dos lado ======= */

const perimeter = ((base*2)+(height*2));
console.log(perimeter);